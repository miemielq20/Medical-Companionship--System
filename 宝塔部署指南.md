# 医疗陪护系统 - 宝塔部署指南

> 服务器公网IP：124.220.16.162  
> 更新时间：2026-05-26

---

## 一、环境准备

在宝塔软件商店安装以下环境：

| 软件 | 版本 |
|---|---|
| Java JDK | 21 |
| MySQL | 8.0+ |
| Redis | 7.0+ |
| Nginx | 宝塔默认 |

---

## 二、数据库初始化

### 1. 创建数据库

宝塔 → 数据库 → 添加数据库

| 参数 | 值 |
|---|---|
| 数据库名 | `mc_sys` |
| 用户名 | `root` |
| 密码 | `aZhTnTG97XKX4JbY` |

### 2. 导入数据

将 `mc_server/src/main/resources/db/mc_sys_init.sql` 导入到 `mc_sys` 数据库。

---

## 三、后端部署

### 1. 本地构建

```bash
cd mc_server
mvn clean package -DskipTests
```

### 2. 上传 JAR 包

将 `mc_server/target/mc_server-0.0.1-SNAPSHOT.jar` 上传到服务器：

```
/www/wwwroot/mc_system/mc_server/mc_server-0.0.1-SNAPSHOT.jar
```

### 3. 停掉旧进程

```bash
# 查看占用 8080 端口的进程
lsof -i :8080

# 一步到位杀掉
kill -9 $(lsof -t -i:8080)
```

### 4. 启动服务

```bash
cd /www/wwwroot/mc_system/mc_server
nohup java -jar mc_server-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod > app.log 2>&1 &
```

> ⚠️ 务必带上 `--spring.profiles.active=prod` 参数

### 5. 验证

```bash
curl http://127.0.0.1:8080/api/Index/index
```

返回正常 JSON 数据即启动成功。

---

## 四、前端部署

### 管理后台（mc_ui）

**技术栈**：Vue 3 + Vite + Element Plus + TypeScript + Axios

#### 构建

```bash
cd mc_ui
npm install
npm run build
```

#### 关键配置文件

**`mc_ui/vite.config.js`**：

```js
export default defineConfig({
  plugins: [
    vue(),
    AutoImport({ resolvers: [ElementPlusResolver()] }),
    Components({ resolvers: [ElementPlusResolver()] }),
  ],
  // 开发环境代理（仅本地开发使用，生产由 Nginx 处理）
  server: {
    historyApiFallback: true,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  },
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  }
})
```

**`mc_ui/.env.production`**：生产环境 API 地址

```
VITE_API_BASE_URL=/api
```

**`mc_ui/src/utils/request.ts`**：Axios 实例配置

```ts
const http = axios.create({
  baseURL: '/api',      // 所有请求统一走 /api 前缀
  timeout: 10000,
})

// 请求拦截器：自动附带 x-token（白名单除外）
const whiteUrls = ['/get/code', '/user/authentication', '/login']

// 响应拦截器：token 过期（code=-2）自动跳转登录页
```

**`mc_ui/src/router/index.ts`**：动态路由，路由列表从后端接口获取并存入 `localStorage`

```ts
const routes = [
  {
    path: '/',
    component: Layout,
    meta: { requiresAuth: true },   // 需要登录
    redirect: () => { /* 动态重定向到第一个菜单路由 */ }
  },
  {
    path: '/login',
    component: LoginView,
    meta: { requiresAuth: false }   // 不需要登录
  }
]
```

#### 上传

将 `mc_ui/dist/` 下所有文件上传到 `/www/wwwroot/mc_ui/`

---

### 移动端（mc_h5）

1. 构建：

```bash
cd mc_h5
npm install
npm run build
```

2. 将 `mc_h5/dist/` 下所有文件上传到 `/www/wwwroot/mc_h5/`

---

## 五、Nginx 配置

### 管理后台站点（端口 80）

宝塔 → 网站 → 添加站点：

| 配置项 | 值 |
|---|---|
| 域名 | `124.220.16.162` |
| 根目录 | `/www/wwwroot/mc_ui` |
| PHP 版本 | 纯静态 |

配置文件内容：

```nginx
server {
    listen       80;
    server_name  124.220.16.162;

    # 前端静态资源（Vue SPA，history 模式需要 try_files）
    location / {
        root   /www/wwwroot/mc_ui;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # ⚠️ 反向代理 /api 到后端 8080，proxy_pass 尾部不能有斜杠！
    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root   /www/wwwroot/mc_ui;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

### 移动端站点（端口 8081）

宝塔 → 网站 → 添加站点：

| 配置项 | 值 |
|---|---|
| 域名 | `124.220.16.162:8081` |
| 根目录 | `/www/wwwroot/mc_h5` |
| PHP 版本 | 纯静态 |

> ⚠️ 需先在 宝塔 → 安全 → 防火墙 放行 `8081` 端口

配置文件内容：

```nginx
server {
    listen       8081;
    server_name  124.220.16.162;

    location / {
        root   /www/wwwroot/mc_h5;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root   /www/wwwroot/mc_h5;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

修改配置后**重启 Nginx**。

---

## 六、请求链路说明

```
浏览器 → Nginx (80端口) → /api/* → 后端 (8080)
                          → /*     → mc_ui 静态文件

浏览器 → Nginx (8081端口) → /api/* → 后端 (8080)
                           → /*     → mc_h5 静态文件
```

---

## 七、后端接口清单

| 接口 | 方法 | 说明 | 需要 Token |
|---|---|---|---|
| `/api/get/code` | POST | 获取短信验证码 | ❌ |
| `/api/user/authentication` | POST | 用户注册 | ❌ |
| `/api/login` | POST | 用户登录 | ❌ |
| `/api/Index/index` | GET | 首页 | ❌ |
| `/api/h5/companion` | GET | H5 陪护师列表 | ❌ |
| `/api/user/getMenu` | GET | 获取用户菜单 | ✅ |
| `/api/user/setMenu` | POST | 设置权限组 | ✅ |
| `/api/auth/admin` | GET | 账号列表 | ✅ |
| `/api/menu/list` | GET | 菜单权限列表 | ✅ |
| `/api/menu/permissions` | GET | 菜单权限结构 | ✅ |
| `/api/menu/selectlist` | GET | 权限组下拉选项 | ✅ |
| `/api/photo/list` | GET | 陪护师头像列表 | ✅ |
| `/api/companion/list` | GET | 陪护师列表 | ✅ |
| `/api/set/companion` | POST | 创建/更新陪护师 | ✅ |
| `/api/companion/delete`| POST | 批量删除陪护师 | ✅ |
| `/api/order/list` | GET | 订单列表 | ✅ |
| `/api/order/detail` | GET | 订单详情 | ✅ |
| `/api/order/complete` | POST | 完成订单 | ✅ |
| `/api/dashboard` | GET | 控制台数据 | ✅ |
| `/api/user/update` | POST | 更新用户信息 | ✅ |

---

## 八、访问地址

| 项目 | 地址 |
|---|---|
| 管理后台 | [http://124.220.16.162](http://124.220.16.162) |
| 移动端 | [http://124.220.16.162:8081](http://124.220.16.162:8081) |
| 后端 API（直连） | [http://124.220.16.162:8080/api](http://124.220.16.162:8080/api) |

---

## 九、常见问题 & 踩坑记录

### 1. 浏览器访问 8080 出现 404

**原因**：应用配置了 `context-path=/api`，根路径 `/` 本身没有页面。

**解决**：通过 Nginx 在 80 端口访问前端，或直接访问具体接口如 `/api/Index/index`。

---

### 2. 前端登录报 404

**原因**：Nginx 的 `proxy_pass` 尾部多了一个斜杠：

```nginx
# ❌ 错误：尾部有斜杠，/api/login 会被转成 /login
proxy_pass http://127.0.0.1:8080/;

# ✅ 正确：尾部无斜杠，/api/login 原样转发
proxy_pass http://127.0.0.1:8080;
```

带斜杠时 Nginx 会截掉 `/api` 前缀，由于后端 context-path 是 `/api`，实际需要 `/api/login`，导致 404。

---

### 3. Nginx 配置重复冲突

**现象**：配置文件中存在多个 `location /api` 块。

**解决**：删除重复的配置块，只保留一个正确的版本。

---

### 4. curl 多行命令换行符失效

**解决**：改用单行命令：

```bash
curl -X POST http://127.0.0.1:8080/api/login -H "Content-Type: application/json" -d '{"userName":"18779039760","passWord":"123456"}'
```

---

### 5. 核心原则

> 后端有 `context-path` 时，Nginx 的 `proxy_pass` 后面**不要带斜杠**！
